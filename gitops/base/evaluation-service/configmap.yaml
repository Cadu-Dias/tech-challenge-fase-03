apiVersion: v1
kind: ConfigMap
metadata:
  name: evaluation-config
  namespace: toggle
data:
  PORT: "8004"
  # Endpoint do ElastiCache Redis criado por infra/modules/elasticache
  # (output "redis_endpoint" do terraform apply). Atualizar aqui se o
  # cluster for recriado (o endpoint muda).
  REDIS_URL: "redis://togglemaster-prod-redis.5pwtyq.0001.use1.cache.amazonaws.com:6379"
  FLAG_SERVICE_URL: "http://flag-service:8002"
  TARGETING_SERVICE_URL: "http://targeting-service:8003"
  AWS_REGION: "us-east-1"
  AWS_SQS_URL: "https://sqs.us-east-1.amazonaws.com/473556687259/toggle-events"
